PYTHON_C_CHEATSHEET = [
    ("len(arr)", "sizeof(arr) / sizeof(arr[0])"),
    ("for i in range(n):", "for (int i = 0; i < n; ++i)"),
    ("if x in arr:", "for (...) { if (arr[i] == x) { ... } }"),
    ("dict.get(k, default)", "hash table lookup with fallback"),
    ("def func(a: int) -> int:", "int func(int a) { ... }"),
]
